# WP-5 (MOTION) — DESIGN-PARITY SCORECARDS

Verifier run: 2026-07-19. Fresh scoring from current renders + metrics only.
Inputs: `WP-5/parity-candidate-dd-editorial/`, `WP-5/parity-candidate-countdown-event/`, `WP-5/js-off/`, frozen `references/{countdown,field-guide,mockup}/render/`, laws verbatim from `WP-2/part1-verbatim.md`.
Shots opened: Candidate 1 — 12 (8 burst frames: 4×1440 + 4×390, cover, 1440/depth-2, 1440/depth-5, 390/depth-3). Candidate 2 — 15 (8 burst frames: 4×1440 + 4×390, cover, 1440/depth-1, 1440/depth-2, 1440/depth-3, 1440/depth-5, 1440/depth-6, 1440/depth-7, 390/depth-2, 390/depth-3, 390/depth-4, dark-1440). References — 5 (countdown 1440/cover, 1440/depth-1, 390/depth-2; field-guide 1440/cover; mockup 1440/depth-2). JS-off pack — 2 (countdown-nojs-390/cover, depth-3). Burst-frame pixel identity checked by md5 across all four sequences.

---

## CANDIDATE 1 — Deep Dive, editorial skin, tier0 print-still
Source under test: `tools/fixtures/skinned/dd-2026-06-30-editorial.html` (per metrics.json)

### The three verbatim questions
- **"Objects on a surface, or boxes on a page?"** — Objects: tilted taped index cards, circular ink stamps with hand-script annotations beside them ("the siege that stopped an Arab conquest…", `1440/depth-2.png`), ghost Roman numerals bleeding off-column (`1440/depth-5.png`, giant "XII"), all sitting on visibly patterned paper grounds.
- **"Does the scroll travel, or is it one flat ground?"** — It travels: dark ink cover → Act I stone/cream crosshatch (`1440/depth-2.png`) → Act II parchment-gold grain (`390/depth-3.png`) → Act III dark ash ground (`1440/depth-5.png`), with `mx-transit` marquee seams between acts (3 marquee events in metrics at y=23003/47650/61821).
- **"Would a reader who loved the trip countdown recognize this as the same magazine's craft?"** — Yes: the same fastener/stamp/transit/ghost-numeral furniture language, rendered in paper-and-ink instead of poster night — recognizably one magazine at a bookish register.

### Laws
| Law | Verdict | Evidence |
|---|---|---|
| 1 Scenography | **PASS** | Every act carries a visible pattern ground (crosshatch grain in `render/1440/depth-2.png` and `render/1440/depth-5.png`; gold grain in `render/390/depth-3.png`); no flat single-color act seen at any of 4 depths inspected. Objects carry ≥2 of tape/tilt/shadow/annotation (taped tilted index card + shadow, `1440/depth-5.png`; stamp + script annotation, `1440/depth-2.png`). Grid-breakers: ghost numerals bleed off-edge ("V" at `burst/1440/burst-0.png`, "XII" at `1440/depth-5.png`); cover ghost "1453"/"Rhomaioi" bleed (`1440/cover.png`). Q1 answered "objects". |
| 2 Density | **PASS** | metrics.json: 84 events, 1.00/screen at 1440 and 0.74/screen at 390 (DD floor 0.7 met at both); words/screen 219 (1440) and 162 (390) ≤ 220 cap; `longestZeroEventRun: 1` and `law2DistributionFail: false` at both widths. Note: 1440 sits just above the 0.9 target end — above target, not below floor; no over-density fail is defined. |
| 3 Length | **PASS** | bodyCopy = 18,429 words at both widths (metrics.json) vs Deep Dive floor 8,000. |
| 4 Arc | **PASS** | Three palette acts + coda, each its own ground (see Q2); transitions are designed `mx-transit` marquee bands ("STONE leaving … GOLD arriving", metrics label at y=23003), not background swaps. Side-by-side with `references/mockup/render/1440/depth-2.png` (act-ground + object furniture de-themed): same language. Editorial arc expressed in ink/paper tints as Law 4 requires. |
| 5 Motion (tier0) | **PASS** | Burst md5s: at both widths frames 0/1/2 differ, 2–5 identical (settled) — motion present, then done. `burst/1440/burst-0.png`: hairline above "File V" only ~150px drawn, ghost "V" offset low; `burst-1.png`: hairline drawn full measure, ghost settled. Same at 390 (`burst/390/burst-0.png` short rule → `burst-1.png` full rule + ghost parallax shift). Subtle-but-present = tier0 PASS. Vocabulary is exclusively tier0: metrics motion kinds are only `CSSAnimation:mx-draw-in` (66) and `CSSTransition:transform` on `.mx-plate__ghost` (26) — no slam/flip/rise anywhere (no over-tier). animatedElements = 46/46 at 1440/390 (spec 46). reducedMotion census = 0 at both widths. JS-off: `js-off/jsoff-dd.json` words 18,429 = 18,429, verdict PASS both widths. Body text: paragraphs pixel-identical and fully opaque in all 6 frames at both widths (e.g. "Justinian I ruled…" block static across `burst/1440/burst-0..5`) — motion attached to furniture only. |
| 6 Object fiction | **PASS** | One archival-paper kit: ruled index cards (23 ephemera), ink stamps ("THE GREAT SIEGE 717–718", `1440/depth-2.png`), ledgers/scorecards (6), attributed quote-objects (8), statbands (6) — facts ship as kit objects, no generic bordered div observed carrying kit-eligible content in any inspected shot. |
| 7 Covers | **PASS** | `1440/cover.png`: owned DD gesture — oversized ghosted numeral "1453" + ghost script "Rhomaioi"; poster-grade with ≥3 voices. Side-by-side same-width: not interchangeable with `references/field-guide/render/1440/cover.png` (giant "44", indigo/pink) nor `references/countdown/render/1440/cover.png` (bubble-letter night poster). |
| 8 Type | **PASS** | Cover carries ≥3 voices (gold script kicker, serif display with outlined "Outlived", mono chips/act pills, sans deck). Body measure ~630px ≈ 60–65ch ≤ 68ch (`1440/depth-2.png`). Mono micro-labels legible at 390 (`390/depth-3.png` CREDIT block); no 0.66rem-class violation observed. Playfulness confined to display/furniture; body disciplined serif. |
| 9 Trust | **PASS** | ≥5 named external voices required; 5 distinct named render as quote-objects (metrics quote labels + `references`-style attribution furniture seen on-page): Justinian I, St Basil of Caesarea, Loukas Notaras, Judith Herrin, Edward Gibbon. Herrin appears 2× (≤2 renderings). No "— THE SIGNAL" self-quote in the quote list; "The Argument · the magazine's take" ledger is ANALYZED-lane with working shown. |
| 10 Craft floor | **PASS** | metrics both widths: `horizontalScroll.flag: false`, `chromeOverlap.fails: false` (0 intersections / 10 depths), `tableTruncation.fails: false`. Fixed pill absent over content in 390 mid-scroll shot (`390/depth-3.png` — hidden on scroll). Token dark mode renders coherently (`render/dark-1440.png`). |
| 11 Identity | **PASS** | Editorial skin: bookish gravitas kept; scenography is paper-and-ink archival (stamps, index cards, ledgers — not theme-park props); motif surface = palette arc + crosshatch pattern + ghost-numeral cover gesture. |
| 12 Honesty | **PASS** | All claims above trace to committed artifacts in `WP-5/parity-candidate-dd-editorial/` and `WP-5/js-off/`; burst identity verified by checksum, not asserted. |

**CANDIDATE 1 PARITY: PASS**

---

## CANDIDATE 2 — Countdown, event skin, tier2
Source under test: `tools/fixtures/skinned/countdown-2026-06-14-event.html` (per metrics.json)

### The three verbatim questions
- **"Objects on a surface, or boxes on a page?"** — Objects, emphatically: taped tilted polaroids overlapping the fold, a circular brass "EFTELING DUSK" stamp, and a rotated marquee tape band bleeding off both edges over a giant ghost "2" — all on a dotted indigo night ground (`render/1440/depth-1.png`).
- **"Does the scroll travel, or is it one flat ground?"** — It travels: indigo starfield night (Part One) crossfades through a dark transit seam with ghost "EFTELING/SAFARI" type and a tilted "SAVANNAH · WILD · NO CAR" plate into cream savannah grain (Part Two) (`render/390/depth-3.png`; seam opacity transitions in metrics motion sample) — the indigo-night → cream-savannah reference arc itself.
- **"Would a reader who loved the trip countdown recognize this as the same magazine's craft?"** — Yes: side-by-side with `references/countdown/render/1440/cover.png` and `1440/depth-1.png`, the candidate keeps the poster cover, starfield-behind-content ambience, flip-clock, postcards and stamps at the same craft level.

### Laws
| Law | Verdict | Evidence |
|---|---|---|
| 1 Scenography | **PASS** | Starfield/dot ground behind content throughout Part One, including behind ranked cards and a pinned chart at 390 (`390/depth-2.png`) — meets the reference ambient standard (`references/countdown/render/390/depth-2.png`, starfield behind plain body text). Objects carry tape + tilt + shadow + overlap (polaroid stack, `1440/depth-1.png`; pinned chart with ball-pin, `390/depth-2.png`). Grid breakers: marquee band bleeding off both edges, stamp overlapping card stack. Savannah act carries grain ground (`1440/depth-7.png`). Q1 answered "objects". |
| 2 Density | **PASS** | metrics: 115 events; 2.19/screen (1440), 1.46/screen (390) — above the 1.0 trip-special floor at both; words/screen 121.66 / 80.94 ≤ 210; `longestZeroEventRun: 1`, `law2DistributionFail: false` both widths. Note: 1440 exceeds the 1.3 target end (dense, not sparse); Law 2 defines failure as emptiness/averages-only, both clean here. |
| 3 Length | **PASS** | bodyCopy = 6,380 words (metrics.json) vs Countdown floor 4,500. |
| 4 Arc | **PASS** | Two acts, each owned ground (starfield night / savannah grain) + designed transit: `mx-transit` marquee at y=20666 ("EFTELING the floating castle … fairytale → savannah"), seam crossfade captured in `390/depth-3.png` and as `CSSTransition:opacity` on `.mx-seam--out/--in` in metrics. Matches the reference countdown's indigo→savannah shift by construction and by eye. |
| 5 Motion (tier2) | **PASS** | Burst evidence at BOTH widths, frames not all identical (md5: frames 0/1/2 distinct, 2–5 settled at each width). 1440: `burst-0.png` shows the shelf region EMPTY except the static body paragraph on bare grain; `burst-1.png` shows polaroids/stamp landed mid-settle (1–3px offsets vs settled `burst-2..5.png`). 390: `burst-0.png` shows the polaroid semi-transparent, mid-tilt, tape not yet placed; `burst-1.png` near-settled; `burst-2..5.png` settled — objects visibly mid-reveal → settled. animatedElements = 144/144 (spec 144); tier2 vocabulary confirmed in metrics kinds: `mx-slam-in` (stamp slams), `mx-flip-in` on countdown numerals (the format's signature flips), `mx-row-tick`, `mx-settle-in`, `mx-rise-in`, `mx-marquee-drift`, seam opacity. reducedMotion census = 0 at both widths. JS-off: `js-off/jsoff-countdown.json` words 6,380 = 6,380 PASS both widths; `js-off/countdown-nojs-390/cover.png` and `depth-3.png` show cover poster, transit band and quotes fully rendered with JS disabled. Running body text: the paragraph in every burst frame ("Lastly the lake and the Resort itself…") is fully opaque and pixel-static at both widths while furniture animates around it — motion on furniture only. No scroll-jacking observed across depth teleports (all depths render normally). |
| 6 Object fiction | **PASS** | One postal/safari kit: "Greetings from…" mail postcards (8), brass/ink stamps with `mx-slam`, taped polaroids, ranked plates with WHERE/WHY ledger rows (`1440/depth-2.png`), flip-clock countdown statband (`1440/depth-7.png`), cheat-sheet ("Five things the brochures bury"). Facts/quotes/data all wear the kit; no generic bordered div carrying kit-eligible content observed. |
| 7 Covers | **PASS** | `1440/cover.png`: poster-grade framed night poster — arc script "greetings from", giant bubble "EFTELING", outlined "BEEKSE BERGEN", starfield + illustration below; ≥3 voices. Same owned gesture family as `references/countdown/render/1440/cover.png` (arc text, moon, castle, flip clock) without being a copy; clearly not interchangeable with the DD cover or field-guide "44". |
| 8 Type | **PASS** | Cover: script + bubble display + outline display + mono chrome (≥3 voices). Mono micro-labels (WHERE/WHY, PHOTO credits) legible at both widths (`390/depth-2.png`); body ragged column ≈ 60ch at 1440 (`1440/depth-3.png`); playfulness lives in poster/furniture, body sans stays disciplined. |
| 9 Trust | **PASS** | Trip specials need ≥6 named external voices. Confirmed verbatim on-screen: Benjiramon (Along for the Ride — 2 renderings, `1440/depth-2.png`), Lisa (FlipFlop Globetrotters, `burst/390/burst-1.png`), Mariska Vermij-Van Dijk (Keeper, via Omroep Brabant — attributed quote-object, `1440/depth-5.png`), the Bayliss family (Little Clogs Holidays, `1440/depth-7.png`). Metrics list 9 quote-objects + 8 mail cards; every quote/mail object inspected (6 of 6) carries a named attribution in the same furniture, and the remaining quote labels are distinct sourced reviews/keeper statements ("We think it's important to match genetically healthy cheetahs…", "Unlike busy, overstimulating theme parks…", etc.) — distinct named voices ≥6. EXPERIENCED lane is carried entirely by these named verbatim quotes; no first-person, no "— THE SIGNAL" self-quote in the event list. |
| 10 Craft floor | **PASS** | metrics both widths: `horizontalScroll.flag: false` (overflow suppressed by design, geometricOverflow false), `chromeOverlap.fails: false` (0 intersections / 10 depths), `tableTruncation.fails: false`. Back pill behavior matches the frozen reference (hide-on-scroll pill appears over content only on scroll-up frames in both candidate and `references/countdown/render/390/depth-2.png` — parity with the bar, and instrumented overlap is 0). Dark render coherent (`render/dark-1440.png`). Ephemera are real photos in polaroid frames. |
| 11 Identity | **PASS** | Event skin at full holiday-descended treatment: poster cover, starfield scene, stamps/slams, marquee, flip-clock countdown open and close (`1440/cover.png`, `1440/depth-7.png`). It is itself — not the editorial skin, not the weekly. |
| 12 Honesty | **PASS** | Every claim above traces to committed artifacts in `WP-5/parity-candidate-countdown-event/` and `WP-5/js-off/`; burst-frame distinctness verified by checksum; mid-reveal states verified by eye at both widths. |

**CANDIDATE 2 PARITY: PASS**

---

### Verifier notes (for the audit trail)
- Burst md5 pattern for all four sequences: frames 0≠1≠2, frames 2=3=4=5. Motion is real and completes within ~840ms of the teleport; the "all 6 identical" failure mode is absent at every width for both candidates.
- Candidate 1's entire animation inventory is `mx-draw-in` + ghost `transform` — nothing above tier0. Candidate 2's inventory includes the format signature (`mx-flip-in` on the countdown) plus slams/settles/rise/marquee/seam — tier2 as specified.
- Densities run above target (not below floor) at 1440 for both candidates; recorded as notes, not fails, since Law 2's stated failure conditions (empty runs, shortness, averages-without-distribution) all pass.
