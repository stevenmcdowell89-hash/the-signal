# WP-3 Design-Parity Scorecards

Verifier: independent design-parity pass, 2026-07-19. Inputs: the two candidate packs (`render/` + `metrics.json`), the frozen reference packs (`references/{countdown,field-guide,mockup}/render/`), and Part 1 verbatim (`WP-2/part1-verbatim.md`). No source HTML/CSS, git history, or progress docs were consulted. Screenshots opened: 13 candidate-1, 11 candidate-2, 9 reference (both widths + dark for each candidate; minimums met).

---

## CANDIDATE 1 — RE-SCORED after words/screen remediation (fresh verifier)

Editorial-skin Deep Dive (`parity-candidate-dd-editorial/`, source `dd-2026-06-30-editorial.html`). Fresh verifier, 2026-07-19, no build context: inputs were the re-rendered pack (render/ regenerated 19:28Z, metrics.json 19:26Z — same pageHeights 75,734 / 95,820, self-consistent), the frozen references, and Part 1 verbatim. All twelve laws re-scored from the CURRENT renders — the spacing change moved every depth sample, so no verdict was inherited. Screenshots opened: 12 candidate (1440 cover/d1/d2/d3/d4/d5/d6/d7 sampled, 390 cover/d1/d2/d4/d6, dark-1440), 4 reference (mockup 1440/depth-2, field-guide 390/depth-2, countdown 1440/cover + depth-4).

### Step 1 — the three questions (current screenshots alone)

- **"Objects on a surface, or boxes on a page?"** Objects on a surface: taped ruled index cards with handwritten margin-note captions, rotated circular date-stamps annotated in a hand voice ("the siege that stopped an Arab conquest of Constantinople"), a push-pinned tilted map card and a push-pinned hand-drawn bar chart, all carrying tilt + shadow on a continuously cross-hatched paper ground (1440/depth-2.png, depth-3.png; 390/depth-2.png, depth-6.png).
- **"Does the scroll travel, or is it one flat ground?"** It travels: dark-ink cover → cream paper Act I (1440/depth-1, depth-2) → parchment-gold Act II (depth-3, depth-4) → dark ash-brown Act III (depth-5) → cream coda (depth-6, depth-7), with three `mx-transit` marquee seams recorded in metrics ("STONE leaving… GOLD arriving" etc.).
- **"Would a reader who loved the trip countdown recognize this as the same magazine's craft?"** Yes — the same tape/pin/stamp fastener language, handwritten annotations, ghost numerals and mono micro-labels the countdown speaks in, translated into archival ink instead of holiday scenography.

### Step 2 — side-by-sides (Laws 1, 2, 4, 7), candidate + reference at the same width in the same view

- **Law 1 (scene), candidate 1440/depth-6 + mockup 1440/depth-2:** the mockup kit standard is a taped index card + CSS solidus card at ~2° with shadows and handwritten captions on a plum ground. The candidate matches it object-for-object in paper terms — taped ruled card with "margin note · File XIII / INDEX CARD · THE SIGNAL ARCHIVE" mono attribution, same tape tab, same tilt-and-shadow — on a cross-hatch ground that is never flat. Quieter ground than the mockup's plum is the correct Law-11 editorial expression, not a gap.
- **Law 2 (density), candidate 390/depth-6 + field-guide 390/depth-2:** field-guide's screen carries one taped photo card over body text (with its back pill overlapping copy); the candidate's screen carries a taped/captioned icon card + mono credit + crosshead inside one screen, no chrome collision. On-screen cadence matches the reference bar; quantitative confirmation below.
- **Law 4 (arc), candidate depth-3/depth-5 + countdown 1440/depth-4:** the countdown's cited shift is indigo-night → cream-savannah; the candidate performs the same travel in ink tints — cream → parchment-gold → dark ash → cream — and its ash act still shows the cross-hatch grain (depth-5), so no act sits on a flat color. Act-for-act, the candidate's grounds are as distinct as the reference's.
- **Law 7 (cover), candidate 1440/cover + countdown 1440/cover:** countdown's poster is arc-text "GREETINGS FROM" + drop-shadow display + castle/sun illustration; the candidate's is the owned Deep Dive gesture — oversized ghost "1453" bleeding off the right edge, ghost script "Rhomaioi", script kicker, serif display with outlined "Outlived", act pills. Both poster-grade, not interchangeable. The countdown reference's own masthead is overlapped by the back pill ("…gnal"); the candidate reserves chrome space cleanly at both widths.

### Step 3 — the Twelve Laws (all re-scored fresh)

| Law | Verdict | Evidence (current renders + metrics) |
|---|---|---|
| 1 Scenography | **PASS** | 1440/depth-2: rotated "GREAT SIEGE 717–718" stamp + handwritten annotation on cross-hatch ground behind plain body text (the ambient standard); depth-3: pinned tilted map card + "MEDIEVAL APEX 1025" stamp + pinned hand-drawn bar chart with pushpin and handwritten caption; depth-1: ghost "V" numeral at the right edge; cover: ghost "1453" bleeding off-edge. Every ephemera object carries ≥2 of rotation/fastener/shadow; no flat single-color act ground in any of the 12 shots, including the dark ash act (depth-5 keeps visible cross-hatch). Objects, not boxes. |
| 2 Density w/ distribution | **PASS** | metrics.json 1440×900: words/screen **219 ≤ 220** Deep Dive cap (bodyCopy 18,429 / 84.15 screens — arithmetic checks: 219.0; the prior 222.7 breach is remediated by real added air: pageHeight grew 75,734 with word count unchanged). 390: 162.33 ≤ 220. Events/screen **1.00** (1440) and **0.74** (390) vs the 0.7 floor — both at or above floor. Distribution: `longestZeroEventRun: 1` at BOTH widths, `law2DistributionFail: false`; empty-screen lists verified by hand — 1440 [0,5,40,49,57,70,73,84] and 390 [0,10,20,…,113] contain no two consecutive screens, so no 3+ zero-event run exists. Screenshot cross-check: the sparsest sampled screen (1440/depth-5, two paragraphs + crosshead) sits between events within one screen either side, consistent with run=1. |
| 3 Length | **PASS** | metrics.json: bodyCopy **18,429** ≥ 8,000 Deep Dive floor (2.3×) — unchanged by the remediation (air was added; words were not cut below floor, nor was density bought by shortness). |
| 4 Arc | **PASS** | Three acts + coda with visibly distinct grounds in the current shots (cream d1/d2 → parchment-gold d3/d4 → dark ash d5 → cream coda d6/d7), expressed in paper/ink tints per Law 4's editorial clause; metrics records 3 `mx-transit` designed seams with leaving/arriving copy ("STONE leaving The Founding Centuries… GOLD arriving", "GOLD… ASH…", "ASH… LIGHT…") at y=23,003 / 47,650 / 61,821 — designed moments, not background swaps. (The 8 depth samples happen to fall between the seams; the seam evidence is the metrics event list, which spot-checks correctly at every position that WAS sampled.) |
| 5 Motion | **PASS** | Tier0 print-still: motion census **0 animated elements** at both widths — correct for tier0 (hairline draw-ins/ghost parallax are the only permitted motions, none required); reduced-motion census 0/0; no motion attached to running text anywhere; nothing in any shot is mid-animation. |
| 6 Object fiction | **PASS** | One archive/dossier kit + universal fasteners: facts as taped ruled index cards with mono "INDEX CARD · THE SIGNAL ARCHIVE" attribution (d4, d6, 390/d2), dates as rotated circular stamps with hand annotations (d2, d3, 390/d4), data as a pinned hand-drawn chart (d3) and ledgers/statbands/scorecards (metrics: ledger ×6, statband ×6, chart ×3). The plainest furniture — depth-7 "Meanwhile" ranked entries — still wears number plates + "NOTED" stamp chips. No generic bordered div carrying kit-eligible content observed. |
| 7 Covers | **PASS** | 1440/cover + 390/cover: the owned Deep Dive gesture (oversized ghost "1453" + ghost script "Rhomaioi"), ≥3 voices (script kicker / serif display / outline "Outlived" / mono strap), act pills; side-by-side with countdown's poster cover: clearly the same magazine, clearly not the same cover. dark-1440.png re-tones the chrome accent (plum → steel-blue) — accent is token-driven, not hard-coded. |
| 8 Type | **PASS** | Six voices present: serif display (titles), outline display ("Outlived"), script/hand ("the Danube frontier, regained", stamp annotations), mono micro-labels ("CREDIT", "INDEX CARD · THE SIGNAL ARCHIVE" — legible at 390, ≥0.72rem by inspection), italic serif decks (File V deck), sans body (cover deck, Meanwhile entries). Body measure ~630px ≈ 60–65ch at 1440 (d1, d2). Cover ≥3 voices. |
| 9 Trust | **PASS** | ≥5 named external voices as quote-objects (metrics: quote ×8): Justinian I ("Solomon, I have outdone thee"), St Basil of Caesarea ("The honour paid to the image passes to the prototype"), Loukas Notaras ("Better the Sultan's turban than the Cardinal's hat"), Judith Herrin (Byzantium: The Surprising Life…), Edward Gibbon ("The triumph of barbarism and religion") — historians/primary sources count for Deep Dive. No "— THE SIGNAL" self-quote in any shot or in the metrics event labels; no quote appears >2× in the event list. |
| 10 Craft floor | **PASS** | metrics 390: document h-scroll `flag:false` (scrollLeftAfterAttempt 0), chrome overlap **0 intersections at 10 depths**, tableTruncation none. 390 shots: masthead stacks and reserves space on the cover; no back-pill overlap on any sampled depth (contrast: both frozen references overlap their own content with the pill); no mid-word truncation; real `<img>` photography and archival images throughout (Ravenna mosaics, icon cards, maps). dark-1440.png renders correctly (cover-only dark capture — noted limitation, nothing contradicted). Contrast comfortably AA in every act including ash-on-cream and cream-on-ash. |
| 11 Identity | **PASS** | Editorial skin stays bookish: paper-and-ink scenography only (cards, stamps, pins, ruled paper — no theme-park props); motif surface capped at palette arc + one pattern (cross-hatch) + one cover gesture (ghost numeral). Recognizably the countdown's craft, spoken in ink. |
| 12 Honesty | **PASS** | Full pack present and internally consistent: render-manifest pageHeights (75,734 / 95,820) match metrics exactly; renders regenerated AFTER the metrics run on the same source; spot-checks of metrics against pixels all land — File V plate y=10,779 ↔ visible at depth-1 (scroll 10,691); great-siege stamp y=21,798 ↔ mid-screen at depth-2 (21,381); SYMPHONIA index card y≈42,673 ↔ depth-4 (42,762); motion census 0 ↔ fully static shots; words/screen arithmetic re-derived independently (18,429/84.15 = 219.0). |

### Verdict

The single prior failure — 222.7 words/screen at 1440×900 vs the 220 Deep Dive cap — is genuinely remediated: same word count, ~2 screens of added air, 219.0 words/screen, verified by independent arithmetic, with distribution still clean (zero-event run of 1 at both widths, event floor met at 0.74/1.00). The spacing change did not regress any other law: act seams, scenography, kit objects, cover, type, voices, and craft floor were all re-verified against the CURRENT renders and hold.

**CANDIDATE 1 PARITY: PASS**

---

## CANDIDATE 2 — event-skin Countdown re-dress (`parity-candidate-countdown-event/`, source `countdown-2026-06-14-event.html`)

### Step 1 — the three questions (screenshots alone)

- **"Objects on a surface, or boxes on a page?"** Objects on a surface: taped, rotated polaroids that overlap; brass circular stamps; a tilted full-bleed caution-tape marquee; airmail-bordered postcards with handwritten quotes — on starfield and savannah-grain grounds (1440/depth-1.png, depth-2.png, depth-4.png, depth-7.png).
- **"Does the scroll travel, or is it one flat ground?"** It travels: indigo-night starfield (Efteling) → cream savannah grain (Beekse Bergen) with a designed transit marquee ("EFTELING… fairytale → savannah") at the seam — the exact indigo-night → cream-savannah shift Law 4 cites (1440/depth-3.png bottom, depth-4.png).
- **"Would a reader who loved the trip countdown recognize this as the same magazine's craft?"** Yes — it is recognizably the same issue, and at rest it is now *cleaner* than the original: same postcard fiction, same countdown, same voices, better-set objects.

### Step 2 — side-by-side vs the ORIGINAL frozen countdown (same widths; pair `1440/depth-5.png` vs reference `1440/depth-5.png` opened in the same message)

- **Law 1:** Both put objects on a scene ground. Re-dress **better**: taped polaroids sit in balanced overlapping shelves with handwritten captions and mono credits (candidate depth-5) where the original hugs the left margin leaving a dead right half (reference depth-5); candidate's savannah ground carries a continuous cross-hatch grain, the original's is near-flat cream with sparse paw-print glyphs. At 390 the candidate keeps the reference's own minimum standard — sparkle glyphs behind plain body text (candidate 390/depth-1.png vs reference 390/depth-2.png).
- **Law 2:** Re-dress **denser and better distributed**: 115 events vs the original's 95; 2.19/screen vs 1.75 (1440) and 1.46 vs 1.15 (390); zero-event run of 1 both widths. Nothing was thinned in the re-dress.
- **Law 4:** Same two-act arc in both. Re-dress **at parity**: the night → savannah shift survives intact and the seam is a designed transit marquee. The original's per-act ambience (constellation lines, drifting glyphs) was busier; the re-dress's grounds are steadier — acceptable, not a regression at rest.
- **Law 7:** Original cover: arc-text "GREETINGS FROM", drop-shadow display, castle + smiling sun; but its chrome overlaps the masthead wordmark ("…gnal" behind the back pill) and at 390 the subtitle/badges collide with the castle art (reference 390/cover.png). Re-dress cover: same poster voice (script "greetings from" + chunky EFTELING + outline BEEKSE BERGEN + naive sun/castle illustration) contained in a postcard rule-frame; no overlap at either width, live-countdown statband kept. Re-dress **better** on craft, **equal** on gesture; one visible change: the accent pill/hairline reads steel-blue where the original was gold — token choice, still `--mx-accent`-driven (identical in dark-1440.png).
- **Where the re-dress is worse / pending:** motion. The original fired **185–187** animated elements during the scroll pass (flips, drifts); the re-dress fires **2** (marquee drift). Scroll-driven tier2 signature moments — stamp-slams, countdown flips, act crossfade — are **pending** (later phase per the engagement brief) and are the one axis where the original still outclasses the re-dress. Also −236 body words vs original (6,380 vs 6,616; still well above floor).

### Step 3 — the Twelve Laws

| Law | Verdict | Evidence |
|---|---|---|
| 1 Scenography | **PASS** | 1440/depth-1.png: taped polaroid ~2° + brass stamp + caution-tape marquee tilted and bleeding both edges + ghost "2" numeral, all on dotted starfield; 390/depth-1.png: sparkle glyphs behind plain body text (the Law-1 reference standard, kept); 1440/depth-4.png: overlapping taped polaroids + "FOUR PARTS ONE PASS" stamp on savannah grain. Every ephemera object carries ≥2 of rotation/fastener/shadow; no flat act ground. |
| 2 Density w/ distribution | **PASS** | metrics.json: events/screen **2.19** (1440) and **1.46** (390) vs 1.0 floor — above the 1.3 target, as the frozen bar itself is (reference 1.75/1.15), so above-target matches the bar; words/screen 121.7 and 81.0, both ≤ 210 max; `longestZeroEventRun: 1` at both widths. |
| 3 Length | **PASS** | metrics.json: bodyCopy **6,380** ≥ 4,500 Countdown floor (−236 vs original 6,616, noted, still 1.4× floor). |
| 4 Arc | **PASS** | Starfield act → grain act with `mx-transit` marquee at the seam (1440/depth-3.png bottom, metrics marquee ×3 incl. "EFTELING the floating castle … fairytale → savannah"); matches Law 4's cited countdown shift. |
| 5 Motion (scoped per engagement: tier2 signature moments deferred) | **PASS (with pending note)** | (i) motion fires during real scroll: metrics `animatedElements: 2` — `mx-marquee__track / CSSAnimation:mx-marquee-drift` at both widths; (ii) reduced-motion census **0** at both widths; (iii) both animated nodes are furniture (marquee tracks), no motion on running text in any shot. **Pending:** fuller tier2 moments — stamp-slam, countdown flips, act crossfade — are absent (original fired 185+ elements); they must land in the later phase for full tier2 parity. |
| 6 Object fiction | **PASS** | Postcard/travel kit throughout: airmail mail-cards with handwritten reader quotes ("Greetings from…", 1440/depth-7.png), taped polaroids, brass circular stamps, ticket-style ranked zig entries with number plates + WHERE/WHY ledger rows (1440/depth-2.png), caution-tape marquee, cheat-sheet ("good to know", metrics), Karibu Town ledger. No generic bordered divs carrying kit-eligible content observed. |
| 7 Covers | **PASS** | cover.png: poster with ≥3 voices (script, chunky solid, outline) + illustration + live countdown statband, framed as a postcard; distinct from field-guide's "44" cover and from the Deep Dive candidate; accent identical in dark-1440.png (token-driven). Re-dress fixes the original's masthead overlap and 390 title collisions. |
| 8 Type | **PASS** | Script kicker, chunky display, outline display, serif italic quote voice, mono micro-labels ("PHOTO · EFTELING PRESS / OFFICIAL SITE" — legible ≥0.72rem, 1440/depth-2.png), sans body. Cover ≥3 voices; body measure ~630px ≈ 65ch (1440/depth-3.png). |
| 9 Trust | **PASS** | ≥6 named external voices, pixel-confirmed four as rendered quote/kit objects: Benjiramon, *Along for the Ride* (zig quotes, 1440/depth-2.png), Lisa, *FlipFlop Globetrotters* (390/depth-4.png), keeper Mariska Vermij-Van Dijk via Omroep Brabant (serif quote-object, 1440/depth-5.png), The Bayliss family, *Little Clogs Holidays* (airmail postcard, 1440/depth-7.png); metrics shows 9 `mx-quote` figures + 8 quote-bearing mail-cards including the Chloe Gallagher "Unlike busy, overstimulating theme parks…" and second-keeper cheetah-EEP quotes carried verbatim from the frozen issue (attributions confirmed in the reference twin, reference 1440/depth-4.png). No self-quote beyond the footer strapline. |
| 10 Craft floor | **PASS** | metrics 390: no document h-scroll, chrome overlap 0/10 depths, no table truncation; 390 shots clean (390/cover.png, depth-1, depth-4); real `<img>` photography throughout; dark-1440.png renders (cover-only capture, noted). Cream-on-indigo and ink-on-cream text comfortably AA. Materially better than the frozen original, whose back pill overlaps content and whose 390 cover self-collides. |
| 11 Identity | **PASS** | Event skin at full scenography: starfield, illustration, caution tape, postcards, stamps — holiday-descended treatment preserved through the re-dress. |
| 12 Honesty | **PASS** | Full pack (manifest, both widths + dark, metrics); metrics spot-checks match pixels (marquee present where census says motion fired; event y-positions match shots; countdown statband present at cover and coda as listed). |

### Verdict

All twelve laws pass under the stated scope (tier2 signature moments explicitly deferred to a later phase — flagged under Law 5 and Step 2 as the one axis where the frozen original still leads).

**CANDIDATE 2 PARITY: PASS**
